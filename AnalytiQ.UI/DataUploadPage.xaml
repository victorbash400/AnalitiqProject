<Page x:Class="AnalytiQ.UI.DataUploadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:effects="clr-namespace:System.Windows.Media.Effects;assembly=PresentationCore"
      Background="Transparent">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Existing Colors, Brushes, Gradients, Shadow Effects, and Styles remain unchanged -->
        <!-- Including all styles from your original XAML here for completeness -->
        <Color x:Key="PrimaryBackColorValue">#121212</Color>
        <Color x:Key="SecondaryBackColorValue">#1E1E1E</Color>
        <Color x:Key="ContentBackColorValue">#242424</Color>
        <Color x:Key="CardBackgroundValue">#1A1A1A</Color>
        <Color x:Key="BorderColorValue">#333333</Color>
        <Color x:Key="PrimaryTextColorValue">#FFFFFF</Color>
        <Color x:Key="MutedTextColorValue">#9E9E9E</Color>
        <Color x:Key="AccentColorValue">#2C9E4B</Color>
        <Color x:Key="SecondaryAccentColorValue">#3ABF5E</Color>
        <Color x:Key="NavHoverColorValue">#2A2A2A</Color>
        <Color x:Key="NavActiveColorValue">#242424</Color>
        <Color x:Key="WarningColorValue">#FF5252</Color>

        <SolidColorBrush x:Key="PrimaryBackColor" Color="{StaticResource PrimaryBackColorValue}"/>
        <SolidColorBrush x:Key="SecondaryBackColor" Color="{StaticResource SecondaryBackColorValue}"/>
        <SolidColorBrush x:Key="ContentBackColor" Color="{StaticResource ContentBackColorValue}"/>
        <SolidColorBrush x:Key="PrimaryTextColor" Color="{StaticResource PrimaryTextColorValue}"/>
        <SolidColorBrush x:Key="MutedTextColor" Color="{StaticResource MutedTextColorValue}"/>
        <SolidColorBrush x:Key="AccentColor" Color="{StaticResource AccentColorValue}"/>
        <SolidColorBrush x:Key="SecondaryAccentColor" Color="{StaticResource SecondaryAccentColorValue}"/>
        <SolidColorBrush x:Key="CardBackground" Color="{StaticResource CardBackgroundValue}"/>
        <SolidColorBrush x:Key="BorderColor" Color="{StaticResource BorderColorValue}"/>
        <SolidColorBrush x:Key="NavHoverColor" Color="{StaticResource NavHoverColorValue}"/>
        <SolidColorBrush x:Key="NavActiveColor" Color="{StaticResource NavActiveColorValue}"/>
        <SolidColorBrush x:Key="WarningColor" Color="{StaticResource WarningColorValue}"/>

        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource AccentColorValue}" Offset="0.0"/>
            <GradientStop Color="{StaticResource SecondaryAccentColorValue}" Offset="1.0"/>
        </LinearGradientBrush>

        <DropShadowEffect x:Key="StandardShadow" ShadowDepth="2" Direction="315" Opacity="0.4" BlurRadius="10"/>
        <DropShadowEffect x:Key="CardShadow" ShadowDepth="1" Direction="315" Opacity="0.3" BlurRadius="8"/>
        <DropShadowEffect x:Key="AccentShadow" ShadowDepth="0" Direction="0" Color="{StaticResource AccentColorValue}" Opacity="0.6" BlurRadius="15"/>

        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextColor}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="{StaticResource AccentColorValue}" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SecondaryAccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource NavHoverColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Background" Value="{StaticResource ContentBackColor}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border" CornerRadius="10" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}" 
                                                     Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                                    <Path Grid.Column="1" Margin="0,0,15,0" Data="M7 10l5 5 5-5z" 
                                          Fill="{StaticResource MutedTextColor}" Width="10" Height="10" Stretch="Uniform"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" 
                                  IsOpen="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                  Placement="Bottom">
                                <Border CornerRadius="10" Background="{StaticResource SecondaryBackColor}" BorderBrush="{StaticResource BorderColor}" 
                                       BorderThickness="1" Margin="0,4,0,0" Effect="{StaticResource CardShadow}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Height" Value="56"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource NavHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextColor}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,24,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBoxBorder" Width="20" Height="20" BorderThickness="1" 
                                   BorderBrush="{StaticResource BorderColor}" CornerRadius="4" Background="{StaticResource ContentBackColor}">
                                <Path x:Name="CheckMark" Data="M4,10 L8,14 L14,5" Stroke="White" StrokeThickness="2" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

        <Style x:Key="ExpanderHeaderStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Padding="4,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" Margin="4,0,0,0" VerticalAlignment="Center" />
                                <Path x:Name="arrow" Grid.Column="1" Stroke="{StaticResource MutedTextColor}" StrokeThickness="1.5"
                                      Data="M 1,1 L 5,5 L 9,1" Width="12" Height="12" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="arrow" Property="Data" Value="M 1,5 L 5,1 L 9,5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextColor}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource AccentColor}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderThickness="0" Background="Transparent">
                                <ToggleButton IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                             Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}"
                                             Style="{StaticResource ExpanderHeaderStyle}" />
                            </Border>
                            <Border Grid.Row="1" Background="Transparent" BorderThickness="0">
                                <ContentPresenter x:Name="ExpanderContent" Visibility="Collapsed" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpanderContent" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <DockPanel Margin="32" LastChildFill="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header with Tenant Info -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,24">
                        <TextBlock Text="Data Upload" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentColor}"/>
                        <TextBlock x:Name="txtTenantInfo" Text="Tenant: Loading..." Foreground="{StaticResource MutedTextColor}" FontSize="14" Margin="0,6,0,0"/>
                    </StackPanel>

                    <!-- Upload Area -->
                    <Border x:Name="UploadArea" Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,24" MinHeight="180" AllowDrop="True">
                        <Grid>
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="24">
                                <Border Width="64" Height="64" Background="{StaticResource ContentBackColor}" CornerRadius="32" Margin="0,0,0,12">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="15" Color="{StaticResource AccentColorValue}" Opacity="0.3"/>
                                    </Border.Effect>
                                    <Path Data="M19.35 10.04C18.67 6.59 15.64 4 12 4 9.11 4 6.6 5.64 5.35 8.04 2.34 8.36 0 10.91 0 14c0 3.31 2.69 6 6 6h13c2.76 0 5-2.24 5-5 0-2.64-2.05-4.78-4.65-4.96zM19 18H6c-2.21 0-4-1.79-4-4 0-2.05 1.53-3.76 3.56-3.97l1.07-.11.5-.95C8.08 7.14 9.94 6 12 6c2.62 0 4.88 1.86 5.39 4.43l.3 1.5 1.53.11c1.56.1 2.78 1.41 2.78 2.96 0 1.65-1.35 3-3 3zm-9-3.82l-2.09-2.09L6.5 13.5 10 17l3.5-3.5-1.41-1.41L10 14.09z"
                                          Fill="{StaticResource AccentColor}" Width="32" Height="32" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Text="Drag and drop files here" FontSize="16" FontWeight="Medium" 
                                          Foreground="{StaticResource PrimaryTextColor}" TextAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="or click to browse" FontSize="13" Foreground="{StaticResource MutedTextColor}" 
                                          TextAlignment="Center" Margin="0,0,0,16"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="SelectFilesButton" Content="Select Files" Style="{StaticResource PrimaryButtonStyle}" 
                                           Width="140" Margin="0,0,8,0"/>
                                    <Button x:Name="UploadButton" Content="Upload Files" Style="{StaticResource PrimaryButtonStyle}" 
                                           Width="140" Margin="8,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Document Type Section -->
                    <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,24">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Document Type:" VerticalAlignment="Center" 
                                          Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,16,0"/>
                                <ComboBox Grid.Column="1" x:Name="DocumentTypeComboBox" Style="{StaticResource ComboBoxStyle}" 
                                         HorizontalAlignment="Stretch" SelectedIndex="0">
                                    <ComboBoxItem Content="Customer Feedback" Foreground="{StaticResource PrimaryTextColor}"/>
                                    <ComboBoxItem Content="Survey Responses" Foreground="{StaticResource PrimaryTextColor}"/>
                                    <ComboBoxItem Content="Support Tickets" Foreground="{StaticResource PrimaryTextColor}"/>
                                    <ComboBoxItem Content="User Reviews" Foreground="{StaticResource PrimaryTextColor}"/>
                                    <ComboBoxItem Content="Other" Foreground="{StaticResource PrimaryTextColor}"/>
                                </ComboBox>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Product:" VerticalAlignment="Center" 
                                          Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,16,0"/>
                                <ComboBox Grid.Column="1" x:Name="ProductComboBox" Style="{StaticResource ComboBoxStyle}" 
                                         HorizontalAlignment="Stretch"/>
                            </Grid>
                            <Expander Grid.Row="2" Header="Processing Options" IsExpanded="False" Margin="0,0,0,0">
                                <Grid Margin="0,16,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="Processing Options:" Style="{StaticResource SubHeaderTextStyle}"/>
                                    <WrapPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
                                        <CheckBox x:Name="AutoCategorizeCheckBox" Content="Auto-categorize" IsChecked="True" 
                                                 Style="{StaticResource CheckBoxStyle}"/>
                                        <CheckBox x:Name="ExtractSentimentsCheckBox" Content="Extract sentiments" IsChecked="True" 
                                                 Style="{StaticResource CheckBoxStyle}"/>
                                        <CheckBox x:Name="GenerateSummaryCheckBox" Content="Generate summary report" IsChecked="False" 
                                                 Style="{StaticResource CheckBoxStyle}" Margin="0,0,0,0"/>
                                    </WrapPanel>
                                </Grid>
                            </Expander>
                        </Grid>
                    </Border>

                    <!-- Action Buttons -->
                    <UniformGrid Grid.Row="3" Columns="1" HorizontalAlignment="Left" Width="160" Margin="0,0,0,24">
                        <Button x:Name="ClearButton" Content="Clear Selection" Style="{StaticResource SecondaryButtonStyle}"/>
                    </UniformGrid>

                    <!-- Processing Status -->
                    <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Padding="0" MinHeight="250">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="24,24,24,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Recent Uploads" Style="{StaticResource SubHeaderTextStyle}" 
                                          Margin="0" VerticalAlignment="Center"/>
                                <Button x:Name="ViewAllButton" Grid.Column="1" Content="View All" Background="Transparent" 
                                       Foreground="{StaticResource AccentColor}" BorderThickness="0" FontWeight="Medium" 
                                       Padding="12,8" VerticalAlignment="Center"/>
                            </Grid>
                            <ListView x:Name="UploadsListView" Grid.Row="1" BorderThickness="0" Background="Transparent" 
                                     ItemContainerStyle="{StaticResource ListViewItemStyle}" Padding="12,0,12,16" 
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48"/>
                                                <ColumnDefinition Width="*" MinWidth="200"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="{StaticResource ContentBackColor}" CornerRadius="8">
                                                <Path Data="M14 2H6c-1.1 0-1.99.9-1.99 2L4 20c0 1.1.89 2 1.99 2H18c1.1 0 2-.9 2-2V8l-6-6zm4 18H6V4h7v5h5v11z" 
                                                     Fill="{StaticResource AccentColor}" Width="20" Height="20" Stretch="Uniform"/>
                                            </Border>
                                            <TextBlock Grid.Column="1" Text="{Binding FileName}" VerticalAlignment="Center" FontWeight="Medium"/>
                                            <TextBlock Grid.Column="2" Text="{Binding UploadTime}" Foreground="{StaticResource MutedTextColor}" 
                                                      VerticalAlignment="Center"/>
                                            <Border Grid.Column="3" Background="{Binding StatusBackground}" Style="{StaticResource StatusBadgeStyle}" 
                                                   VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontSize="12" FontWeight="Medium"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                </Grid>
            </DockPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Value="{Binding UploadProgress}" Maximum="100" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding LoadingMessage}" Foreground="White" FontSize="14" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>